on_actions = {
    on_startup = {
        effect = {
            CZE = {
                puppet = {
                    target = PRA
                    end_wars = no
                }
            }
        }
    }

    # ROOT is the capitulated country; FROM is the winner.
    on_capitulation = {
        effect = {
            if = {
                limit = {
                    tag = BEN
                    country_exists = PRA
                    PRA = {
                        has_country_flag = pra_declared_independence
                    }
                }
                PRA = {
                    set_country_flag = pra_benesov_crisis_resolved
                    country_event = { id = pra_mohammedis.6 }
                }
            }
        }
    }
}
